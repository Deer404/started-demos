{
    "name": "trpc-nextjs-monorepo",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "pnpm run --parallel dev",
        "build": "pnpm run --parallel build",
        "start": "pnpm run --parallel start",
        "lint": "pnpm run --parallel lint"
    },
    "devDependencies": {
        "@types/node": "^20",
        "@types/react": "^18",
        "@types/react-dom": "^18",
        "eslint": "^8",
        "postcss": "^8",
        "tailwindcss": "^3.4.1",
        "typescript": "^5"
    }
}